---
"@sdcorejs/nestjs": patch
---

Validate DOCX, XLSX and PPTX containers with bounded ZIP inspection, allow consumers to narrow upload limits per call, participate in caller-owned file-claim transactions, and authorize exact attached-file reads through a deny-by-default policy.
