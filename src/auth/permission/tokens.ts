/** DI token for `IPermissionStrategy`. */
export const PERMISSION_STRATEGY = Symbol('PERMISSION_STRATEGY');

/** Reflect metadata key under which `@HasPermission` / `@HasAnyPermission` store the required codes. */
export const PERMISSION_METADATA_KEY = 'sdcore:permission:any';
